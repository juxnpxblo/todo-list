{
  "main": "index.ts",
  "scripts": {
    "start": "ts-node server/src/",
    "build": "cd server && npm install && cd ../client && npm install && npm run build"
  },
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "ts-node": "^10.7.0"
  }
}
